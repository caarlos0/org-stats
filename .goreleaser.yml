builds:
- env:
  - CGO_ENABLED=0
  - GO111MODULE=on
brew:
  github:
    owner: caarlos0
    name: homebrew-tap
  folder: Formula
  description: Get the contributor stats summary from all repos of any given organization
nfpm:
  formats:
    - deb
